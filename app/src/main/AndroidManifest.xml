<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_icon_app"
        android:roundIcon="@drawable/logo_icon_app"
        android:supportsRtl="true"
        android:label="@string/app_name"
        tools:targetApi="31"
        >
        <activity
            android:name=".presentation.singIn.MainSingInActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.THI_Comand_work"/>
        <activity
            android:name=".presentation.auth.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
